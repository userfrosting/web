{% extends 'partials/base.html.twig' %}

{% block content %}
    <div id="body-inner">
        <h1>{{ page.title }}</h1>
        <p>
            {{ page.content|raw }}
        </p>
    </div>
{% endblock %}
